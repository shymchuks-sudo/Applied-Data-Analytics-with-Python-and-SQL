-- DQL commands are used to retrieve data from the database. The primary DQL command is SELECT.

-- SELECT: Retrieves data from the database.
SELECT FirstName, LastName
FROM Employees
WHERE Department = 'HR
