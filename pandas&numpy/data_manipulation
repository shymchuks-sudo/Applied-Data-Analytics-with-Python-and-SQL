import pandas as pd
import numpy as np

df = pd.read_csv('dibets_sample.csv')
# check if there are any null values
df.isnull()
# check for nul values in the Insulin column
df.Insulin.isnull()

df.Insulin = df.Insulin.fillna(df.Insulin.mean())
# check for duplicate rows
df.duplicated()
df.duplicated(['PatientID'])
